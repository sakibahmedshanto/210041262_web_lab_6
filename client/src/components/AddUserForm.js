import React from 'react';
import { useNavigate } from 'react-router-dom';

const AddUserButton = () => {
    const [user, setUser] = React.useState({
        name: '',
        email: '',
        age: '',
        password: ''
    });

    return (
       <form>
        
       </form>
    );
}